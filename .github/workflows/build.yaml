name: Build App

on:
    push:
        branches:
        - main

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v2
        - name: build-push
          uses: docker/build-push-action@v1
          with:
            username: ${{ secrets.DOCKER_USERNAME }}
            password: ${{ secrets.DOCKER_PASSWORD }}
            registry: docker.io
<<<<<<< HEAD
            repository: ${{ secrets.DOCKER_USERNAME }}/cd
            tag_with_sha: true
=======
            repository: <your-dockerhub-username>/cd
            tag_with_sha: true
>>>>>>> 4927b15a8bea03c18ae323bf3f8b3ae7e6b0dfff
